{{ $align := .Get "align" }}

<div class="w-full grid grid-cols-12 gap-x-2.5 gap-y-6 lg:gap-6 xl:gap-8">
  <div class="prose
  {{ with eq $align "right" }}
    col-span-full lg:col-span-8 lg:col-start-5 xl:col-span-4 xl:col-start-5
  {{ end }}
  {{ with eq $align "left" }}
    col-span-full lg:col-span-8 xl:col-span-4
  {{ end }}">
    {{ .Inner | markdownify }}
  </div>
</div>